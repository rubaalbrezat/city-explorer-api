//====================================================( FUNCTIONS )=============================================================

function handleMain(req, res) {
    res.status(500).send("Not found");
}

//=====================================================( EXPORTS )=============================================================

module.exports = { handleMain };